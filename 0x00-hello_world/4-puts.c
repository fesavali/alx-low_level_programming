#include <stdio.h>

int main(void)
	/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
	
{
	char text[] = "\n\"Programming is like building a multilingual puzzle";

	puts(text);
	
	/* Return: Always 0 (Success) */
	return (0);
}
