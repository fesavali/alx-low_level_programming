#include <stdio.h>

int main(void)
	
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
	
{
	char text[] = "\"Programming is like building a multilingual puzzle\n";

	puts(text);
	
	/* Return: Always 0 (Success) */
	return (0);
}
