#include <stdio.h>

/**
 * main function
 *
 * use printf
 */
int main(void)
{
	String text = "\n with proper grammar, but the outcome is a piece of art,";
	printf(text);
	
	return (0);
}
